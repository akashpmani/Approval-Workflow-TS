/**
 * LoginPage.tsx — /login route.
 *
 * Purpose:
 *   Thin page component. Centers the <LoginForm /> on the screen.
 *   No business logic — that lives in features/auth.
 *
 * Rendered by app/router.tsx as a public (unauthenticated) route.
 */
