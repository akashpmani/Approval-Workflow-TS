/**
 * DashboardPage.tsx — /dashboard route.
 *
 * Purpose:
 *   Landing page after login. Wraps content in <AppShell />.
 *   Typically shows KPIs, recent approvals, pending actions.
 *
 * Protected route — only renders for authenticated users.
 */
