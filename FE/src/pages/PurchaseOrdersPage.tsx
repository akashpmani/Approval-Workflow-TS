/**
 * PurchaseOrdersPage.tsx — /purchase-orders route.
 *
 * Purpose:
 *   Renders <PurchaseOrderList /> inside <AppShell />. May also show
 *   a "New PO" button that opens <PurchaseOrderForm /> in a modal.
 *
 * Keep this file thin — heavy lifting belongs in feature components.
 */
