/**
 * types/common.ts — primitive/utility types reused across features.
 *
 * Purpose:
 *   Tiny building-block types that aren't specific to any one domain.
 *
 * Typical contents:
 *   - Id, Nullable<T>, Maybe<T>
 *   - Timestamps (createdAt, updatedAt) mixin
 *   - Option<T> for select/autocomplete inputs
 *
 * Rules:
 *   - Keep this file small. If a type belongs to a feature, put it there.
 */
