/**
 * EmptyState.tsx — reusable empty/zero-data placeholder.
 *
 * Purpose:
 *   A consistent visual when a list, table, or screen has no content
 *   to show (e.g. "No purchase orders yet").
 *
 * Props (suggested):
 *   - title:       main message
 *   - description: optional secondary text
 *   - action:      optional React node (e.g. a "Create" button)
 *
 * Lives in components/common/ — generic, not tied to any feature.
 */
