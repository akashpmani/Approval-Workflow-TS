/**
 * LoginForm.tsx — email/password sign-in form.
 *
 * Purpose:
 *   UI-only. Calls the login() action from useAuth(). Handles local
 *   form state + validation; the hook handles API + error state.
 *
 * Rendered by pages/LoginPage.tsx.
 *
 * Consider wiring it to react-hook-form + zod for validation.
 */
