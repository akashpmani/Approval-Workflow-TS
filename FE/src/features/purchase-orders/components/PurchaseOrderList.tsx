/**
 * PurchaseOrderList.tsx — renders the list of purchase orders.
 *
 * Purpose:
 *   Presentational component. Reads data from usePurchaseOrders() and
 *   renders rows (title, amount, status, action buttons).
 *
 * Used by: pages/PurchaseOrdersPage.tsx.
 *
 * Consider splitting a <PurchaseOrderRow /> child component once row
 * rendering grows beyond a handful of lines.
 */
