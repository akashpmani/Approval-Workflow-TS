/**
 * features/purchase-orders/types/index.ts — PO domain types.
 *
 * Purpose:
 *   Types for the Purchase Order feature, co-located with its code.
 *
 * Typical contents:
 *   - PurchaseOrder                 — full entity shape
 *   - CreatePurchaseOrderPayload    — POST body
 *   - UpdatePurchaseOrderPayload    — PATCH body (usually Partial<Create...>)
 *   - PurchaseOrderFilters          — list query params
 */
