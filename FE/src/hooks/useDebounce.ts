/**
 * useDebounce.ts — shared React hook.
 *
 * Purpose:
 *   Returns a debounced copy of a value that only updates after the
 *   given delay has elapsed without changes. Handy for search inputs.
 *
 * Lives in src/hooks/ because it's generic — NOT tied to any feature.
 * Feature-specific hooks live in features/<name>/hooks/.
 */
